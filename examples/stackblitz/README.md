# ng-simple-state example

Minimal Angular app using `ng-simple-state` from npm. Run it in the browser:
**https://stackblitz.com/github/nigrosimone/ng-simple-state/tree/main/examples/stackblitz**

Locally: `npm install && npm start`.
